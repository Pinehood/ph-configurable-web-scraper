export enum ApiRoutes {
  BASE = "/api",
  SCRAPER_SINGLE = "scraper/:name",
  SCRAPER_LIST = "scraper",
  HISTORY = "history",
  JOB = "job/:name",
}

export enum AppRoutes {
  BASE = "/app",
  HOME = "home",
  CREATE = "create",
  JOBS = "jobs",
  SCRAPERS = "scrapers",
  HISTORY = "history",
}
